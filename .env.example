ACTUAL_API_MAJOR=25

# Shared Actual connection (fill in your values)
ACTUAL_SERVER_URL=
ACTUAL_PASSWORD=
ACTUAL_SYNC_ID=
NODE_TLS_REJECT_UNAUTHORIZED=

# Per-service ports (used by stack/docker-compose.yml)
EVENTS_HTTP_PORT=4000
CATEGORISE_HTTP_PORT=3001
INVEST_SYNC_HTTP_PORT=3002
LANDG_HTTP_PORT=3003
MONZO_HTTP_PORT=3005
# TX_LINKER_HAS_NO_PORT=1

# Notes
# - This file controls only the API line used by images in this stack and optional host port mappings.
# - Each service still reads its own app configuration from its repo-level .env (e.g. ../actual-events/.env).
