## Select which image tag to pull for all services
## Set to one of the current stable @actual-app/api tags published by actual-auto-ci, or leave unset to follow 'latest'.
# Examples: 26.1.0 or 26.0.2 or 25.3.4
ACTUAL_IMAGE_TAG=

# Shared Actual connection (fill in your values)
ACTUAL_SERVER_URL=
ACTUAL_PASSWORD=
ACTUAL_SYNC_ID=
NODE_TLS_REJECT_UNAUTHORIZED=

# Shared events integration (used by categorise, reconcile, tx-linker)
# Set these to enable event-driven behavior across services.
ENABLE_EVENTS=
# Example: http://actual-events:${EVENTS_HTTP_PORT:-3000}/events or https://events.example.com/events
EVENTS_URL=
EVENTS_AUTH_TOKEN=

# Shared Traefik proxy
STACK_HTTP_PORT=3000

# Per-service ports (used by stack/docker-compose.yml)
EVENTS_HTTP_PORT=3000
CATEGORISE_HTTP_PORT=3001

# Shared auth gateway configuration
AUTH_FORWARD_IMAGE=ghcr.io/rjlee/actual-auto-auth:latest
SESSION_SECRET=changeme
AUTH_FORWARD_COOKIE_NAME=actual-auth
CATEGORISE_LOGIN_NAME=Actual Auto Categorise
CATEGORISE_AUTH_COOKIE_NAME=categorise-auth

# Example ports for optional services enabled via stack/includes/*.yml
# Uncomment if you enable the corresponding include file
# INVEST_SYNC_HTTP_PORT=3002
# LANDG_HTTP_PORT=3003
# MONZO_HTTP_PORT=3005

# Notes
# - This file controls the image tag for all services in this stack.
# - We publish stable @actual-app/api tags plus 'latest' (see release strategy in rjlee/actual-auto-ci).
# - Per-service overrides live in `stack/env/<service>.env` and take precedence over values here.
